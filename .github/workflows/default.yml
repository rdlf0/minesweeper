name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    #- uses: actions/checkout@v1
    - name: My first step
        uses: actions/hello-world@master
    - run: echo All Done!
